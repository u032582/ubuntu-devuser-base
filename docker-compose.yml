version: "3.0"
services:
  serv:
    image: ubuntu-devuser:latest
    build:
      context: .
      args:
        - USERNAME=devuser
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - $HOME/.Xauthority:/root/.Xauthority
